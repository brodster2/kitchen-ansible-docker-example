---
driver:
  name: docker
  use_sudo: false

provisioner:
  name: ansible_playbook
  playbook: default.yml
  hosts: all

platforms:
  - name: centos
    driver_config:
      image: centos:centos7

suites:
  - name: default
